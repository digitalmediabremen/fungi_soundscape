node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Credentials / local env (GBIF_USER, GBIF_PWD, GBIF_EMAIL). Never commit.
.env
.env.*
!.env.example

# Generated WASM synth (built from of_app/.../ofxPDSP via wasm/build.sh)
src/audio/wasm

# Generated by the build scripts — regenerate with `npm run build:data` /
# `npm run build:gbif`. (Large; not committed. Downloaded dumps/archives are
# cached under .data-cache/ too.)
.data-cache
public/data
public/gbif
